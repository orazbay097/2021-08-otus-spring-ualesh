spring:
  datasource:
    url: jdbc:h2:mem:testdb

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

#    show-sql: true
  sql:
    init:
      mode: always

logging:
  level:
    ROOT: ERROR