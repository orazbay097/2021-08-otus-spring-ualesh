spring:
  datasource:
    url: jdbc:h2:mem:testdb

  jpa:
    generate-ddl: false
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
    hibernate:
      ddl-auto: none

  #    show-sql: true
  sql:
    init:
      mode: always

logging:
  level:
    ROOT: ERROR